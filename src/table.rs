struct RecordIterator {
    curr : Record,
    row_number : i64,
    String : table_name
}

impl Iterator for RecordIterator {
    
    fn next(&mut self) -> Option<i_64> {
        row_number += 1;
        curr = Record::new() 
    } 
}